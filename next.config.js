/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig
module.exports = {
  swcMinify: true,
  // i18n: {
  //   locales: ["en"], 
  //   defaultLocale: "en",
  // },
};